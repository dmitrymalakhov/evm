# Проверка готовности системы

## ✅ Статус проверки

### База данных
- ✅ База данных существует: `api/sqlite/evm.sqlite`
- ✅ Поле `status` добавлено в таблицу `users`
- ✅ Все миграции применены (0000-0005)
- ✅ Миграции применяются автоматически при запуске API

### API сервер
- ✅ Маршрут `/telegram/register` создан
- ✅ Сервис `telegram-auth.ts` создан
- ✅ Маршрут подключен в `routes/index.ts`
- ✅ Валидация данных через Zod

### Telegram бот
- ✅ Структура проекта создана
- ✅ Основные файлы на месте:
  - `src/index.ts` - точка входа
  - `src/bot.ts` - логика бота
  - `src/api-client.ts` - клиент API
- ✅ Команды реализованы:
  - `/start` - регистрация
  - `/myinfo` - получение данных
  - `/cancel` - отмена
  - `/help` - справка

## 🚀 Инструкция по запуску

### 1. API сервер

```bash
cd api
pnpm install  # если еще не установлено
pnpm dev      # запуск в режиме разработки
```

API будет доступен по адресу: `http://localhost:4000`

**Проверка работы API:**
```bash
curl http://localhost:4000/health
```

### 2. Telegram бот

```bash
cd telegram-bot
pnpm install  # если еще не установлено

# Создайте файл .env
cat > .env << EOF
BOT_TOKEN=your_bot_token_from_botfather
API_BASE_URL=http://localhost:4000
EOF

# Запустите бота
pnpm dev
```

**Получение токена бота:**
1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен в `.env`

### 3. Проверка работы

1. **Проверка API:**
   ```bash
   curl -X POST http://localhost:4000/telegram/register \
     -H "Content-Type: application/json" \
     -d '{
       "telegramId": "123456789",
       "firstName": "Test",
       "lastName": "User",
       "phoneNumber": "+1234567890"
     }'
   ```

2. **Проверка бота:**
   - Откройте вашего бота в Telegram
   - Отправьте команду `/start`
   - Поделитесь контактом
   - Получите данные для входа

## 📋 Структура миграций

```
api/drizzle/migrations/
├── 0000_init.sql                 # Базовая структура БД
├── 0001_add_idea_votes.sql      # Голосование за идеи
├── 0002_add_iterations_and_progress.sql  # Итерации и прогресс
├── 0003_add_user_actions.sql    # Действия пользователей
├── 0004_add_secret_santa.sql    # Секретный Санта
└── 0005_add_user_status.sql     # Статус пользователей (для Telegram регистрации)
```

**Важно:** Миграции применяются автоматически при запуске API сервера через `api/src/db/client.ts`

## 🔍 Проверка базы данных

Если нужно проверить структуру базы данных вручную:

```bash
cd api
sqlite3 sqlite/evm.sqlite ".schema users"
```

Должна отображаться таблица `users` с полем `status text DEFAULT 'active'`.

## ⚠️ Возможные проблемы

1. **База данных не создается:**
   - Убедитесь, что папка `api/sqlite/` существует и доступна для записи
   - Проверьте логи API при запуске

2. **Миграции не применяются:**
   - Проверьте наличие папки `api/drizzle/migrations/`
   - Убедитесь, что переменная окружения `DRIZZLE_MIGRATE` не установлена в `false`

3. **Telegram бот не работает:**
   - Проверьте, что токен правильный в `.env`
   - Убедитесь, что API сервер запущен и доступен
   - Проверьте логи бота на наличие ошибок

4. **Ошибки при регистрации:**
   - Проверьте, что API сервер запущен
   - Убедитесь, что URL API правильный в `telegram-bot/.env`
   - Проверьте логи API и бота

## ✨ Все готово к использованию!

Система полностью готова. Все необходимые компоненты созданы и настроены.


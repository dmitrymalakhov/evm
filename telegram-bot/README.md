# Telegram Bot для регистрации пользователей E.V.M.

Telegram бот для регистрации пользователей в системе E.V.M. через API.

## Установка

1. Установите зависимости:
```bash
pnpm install
```

2. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

3. Получите токен бота у [@BotFather](https://t.me/BotFather) в Telegram и добавьте его в `.env`:
```
BOT_TOKEN=your_bot_token_here
```

4. Настройте API_BASE_URL, если API работает на другом адресе:
```
API_BASE_URL=http://localhost:4000
```

## Запуск

### Разработка
```bash
pnpm dev
```

### Production
```bash
pnpm build
pnpm start
```

## Использование

### Команды бота

**Обычные пользователи:**
- `/start` - Начать регистрацию или перезапустить бота
- `/myinfo` - Получить свои данные для входа (табельный номер и пароль)
- `/cancel` - Отменить текущую регистрацию
- `/help` - Показать справку по командам

**Администраторы (ID: 92174505):**
- `/admin` - Админская панель
  - 📢 Рассылка - Разослать сообщение всем зарегистрированным пользователям
  - 📊 Статистика - Просмотр статистики пользователей
  - 🔄 Обновить список - Обновить список пользователей

### Процесс регистрации

1. Пользователь отправляет команду `/start` боту
2. Бот просит поделиться контактом
3. Пользователь делится контактом через кнопку
4. Бот получает данные из контакта (имя, фамилия, номер телефона) и из профиля Telegram (username)
5. Бот регистрирует пользователя через API
6. Бот отправляет пользователю табельный номер и OTP код для входа в систему

### Улучшения

- ✅ Автоматическая проверка уже зарегистрированных пользователей
- ✅ Команда `/myinfo` для повторного получения данных
- ✅ Улучшенная обработка ошибок с понятными сообщениями
- ✅ Retry логика для API запросов с exponential backoff
- ✅ Таймауты для API запросов
- ✅ Логирование всех действий для отладки
- ✅ Inline кнопки для удобной навигации
- ✅ Валидация данных контакта
- ✅ Красивое форматирование сообщений с HTML
- ✅ Graceful shutdown при остановке бота
- ✅ Админская панель с командами для рассылки сообщений
- ✅ Статистика пользователей для администраторов

## API Endpoints

Бот использует следующие endpoints API:
- `POST /telegram/register` - Регистрация пользователя через Telegram
- `GET /telegram/users` - Получение списка всех зарегистрированных пользователей (для рассылки)

## Структура проекта

```
telegram-bot/
├── src/
│   ├── index.ts          # Точка входа
│   ├── bot.ts            # Основная логика бота
│   └── api-client.ts     # Клиент для работы с API
├── package.json
├── tsconfig.json
└── README.md
```

